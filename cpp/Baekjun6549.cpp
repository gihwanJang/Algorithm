#include <cstdio>
using namespace std;

int n, nums[100000];
long ans;

int main(int argc, char const *argv[]){
    while(true){
        scanf("%d", &n);
        if(n == 0) break;
        for (int i = 0; i < n; ++i)
            scanf("%d", &nums[i]);
        
    }
    return 0;
}
