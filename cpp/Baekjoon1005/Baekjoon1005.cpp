#include <cstdio>
#include <vector>
using namespace std;

int main(int argc, char const *argv[]){
    int T, n, k, time[10001];
    scanf("%d", &T);
    while(T--){
        scanf("%d %d", &n, &k);
        for(int i = 1; i <= n; ++i)
            scanf("%d", &time[i]);
        
    }
    return 0;
}
